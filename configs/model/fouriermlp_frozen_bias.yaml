_target_: adjoint_samplers.components.model_variants.FrozenBiasFourierMLP
dim: ${dim}
num_layers: 4
channels: 64
bias_scale: 0.1
bias_seed: 42
activation:
  _target_: torch.nn.GELU
last_bias_init:
  _target_: torch.nn.init.zeros_
  _partial_: True
last_weight_init:
  _target_: torch.nn.init.zeros_
  _partial_: True
