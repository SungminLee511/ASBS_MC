_target_: adjoint_samplers.components.model_variants.ModeAwareFourierMLP
dim: ${dim}
num_layers: 4
channels: 64
split_dim: 0
split_value: 0.0
activation:
  _target_: torch.nn.GELU
last_bias_init:
  _target_: torch.nn.init.zeros_
  _partial_: True
last_weight_init:
  _target_: torch.nn.init.zeros_
  _partial_: True
